using TECS;
using TECS.Commands;

namespace Breakout;
public class StartupSystem:ISystem
{
    public void Run(IEngine engine, CommandBuffer cmd)
    {
        int blockRows = 5;
        int blockCols = 5;
        
        
    }
}